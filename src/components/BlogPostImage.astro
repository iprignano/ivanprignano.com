---
import { Image } from 'astro:assets';

const { src, ...attrs } = Astro.props;
---

<Image src={src} {...attrs} />

<style>
  img {
    margin-block: 2.5rem;
    border-radius: 10px;
    max-width: 100%;
    height: auto;
    border: 1px solid rgba(0, 0, 0, 0.1);
  }
</style>
